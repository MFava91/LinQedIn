#ifndef LINQCLIENT_H
#define LINQCLIENT_H

#include"utente.h"
#include"db.h"

class LinqClient
{
public:
    LinqClient();
    /*
    Utente* u;
    DB* db;
    LinqClient(Username s){
        //carica DB
        db->load()
        u=db->find(s);
    }
    void aggiornaProfilo();
    void showProfilo();
    void insertRete(Username un);
    void removeRete(Username);
    */
};

#endif // LINQCLIENT_H
